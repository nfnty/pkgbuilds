diff --git a/tty.c b/tty.c
index 7be952c..cffbc2b 100644
--- a/tty.c
+++ b/tty.c
@@ -213,6 +213,7 @@ void
 tty_start_tty(struct tty *tty)
 {
 	tty_init_termios(tty->fd, &tty->tio, tty->event);
+	tty->tio.c_iflag &= ~(IXON|IXOFF);
 
 	tty_putcode(tty, TTYC_SMCUP);
 
